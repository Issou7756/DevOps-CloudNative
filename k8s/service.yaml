apiVersion: v1
kind: Service
metadata:
  name: carservice
  labels:
    app: carservice
spec:
  type: NodePort
  selector:
    app: carservice
  ports:
    - name: http
      port: 8080
      targetPort: 8080
